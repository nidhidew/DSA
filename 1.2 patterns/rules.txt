1.  to create a pattern use nested loops, outer and inner. Outer loops for lines/rows and inner loop is for columns and connect them somehow to the rows.
2.  print(* for example) inside the inner loop
3.  observe symmetry(optional)